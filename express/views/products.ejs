<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }
        .hero {
            background-image: url('/img/hero.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            color: white;
            padding: 100px 0;
            text-align: center;
            width: 100%;
            height: 380px; /* Adjust height as needed */
            position: relative;
        }
        .hero-content {
            position: absolute;
            top: 50%;
            left: 10%;
            transform: translateY(-50%);
            color: white;
            max-width: 40%;
        }
        .hero-content h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }
        .hero-content p {
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }
        .hero-content button {
            font-size: 1rem;
            padding: 0.5rem 1rem;
            border: none;
            background-color: #5e5073;
            color: white;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .hero-content button:hover {
            background-color: #4d84e2;
        }
        .categories {
            margin-top: 20px;
            text-align: center;
            width: 100%;
            padding: 0 15px; /* Add padding to avoid white spaces */
        }
        .category-item {
            margin-bottom: 20px;
        }
        .category-item img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 8px;
        }
        .products {
            padding: 50px 0;
        }
        .product-card {
            position: relative;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #ddd;
            border-radius: 8px;
            overflow: hidden;
            transition: all 0.3s ease-in-out;
        }
        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 16px rgba(0, 0, 0, 0.2);
        }
        .product-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        .product-card-body {
            padding: 20px;
            text-align: center;
        }
        .product-card-title {
            font-size: 1.5rem;
            color: #333;
        }
        .product-card-text {
            color: #666;
        }
        .product-card-price {
            font-size: 1.2rem;
            color: #333;
            margin-top: 10px;
        }
        .product-description {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            background-color: rgb(94 80 115);
            color: white;
            padding: 10px;
            transform: translateY(100%);
            transition: transform 0.3s ease-in-out;
            text-align: left;
        }
        .product-card:hover .product-description {
            transform: translateY(0);
        }
        .banner-container {
            width: 100%;
            position: relative;
        }
        .banner-content {
            position: absolute;
            top: 50%;
            left: 10%;
            transform: translateY(-50%);
            color: black;
            max-width: 40%;
        }
        .banner-content h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
        }
        .banner-content p {
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }
        .banner-content button {
            font-size: 1rem;
            padding: 0.5rem 1rem;
            border: none;
            background-color: #7c678a;
            color: white;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .banner-content button:hover {
            background-color: #5e5073;
        }
        .banner-container img {
            width: 100%;
            height: auto;
            display: block;
        }
        #para p {
            font-size: 1.5rem;
            border: 1px solid black;
            border-radius: 3px;
        }
        .navbar{
 background-color: #ffffffb0;;
  
  }
    </style>
    <title>Products</title>
</head>
<body>
    <div class="hero">
        <div class="hero-content">
            <h1>Welcome to Our Plant Store</h1>
            <p>Discover the best plants for your home and garden</p>
            <button class="btn btn-primary">Shop Now</button>
        </div>
    </div>

    <div class="container-fluid categories">
        <div class="row" id="para">
            <div class="col-md-3 category-item">
                <img src="/img/category1.png" alt="Category 1">
                <p>Big Sale Products</p>
            </div>
            <div class="col-md-3 category-item">
                <img src="/img/category2.png" alt="Category 2">
                <p>New Arrivals</p>
            </div>
            <div class="col-md-3 category-item">
                <img src="/img/category3.png" alt="Category 3">
                <p>Top Products</p>
            </div>
            <div class="col-md-3 category-item">
                <img src="/img/category4.png" alt="Category 4">
                <p>Trending Products</p>
            </div>
        </div>
    </div>

    <div class="container products">
        <h2 class="text-center">Our Products</h2>
        <div class="row">
            <% for(let index = 0; index < products.length; index++) { %>
                <div class="col-md-3 mb-4">
                    <div class="product-card">
                        <img src="<%= (products[index].images && products[index].images.length > 0) ? products[index].images[0].url : '/path/to/default-image.jpg' %>" alt="<%= (products[index].images && products[index].images.length > 0) ? products[index].images[0].description : 'No image available' %>">
                        <div class="product-card-body">
                            <h5 class="product-card-title"><%= products[index].name %></h5>
                            <p class="product-card-text">Species: <%= products[index].species %></p>
                            <p class="product-card-price">$<%= products[index].price %></p>
                        </div>
                        <div class="product-description">
                            <p><%= products[index].description %></p>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <% for(let i = 1; i <= totalPages; i++) { %>
                    <li class="page-item <%= (i === page) ? 'active' : '' %>">
                        <a class="page-link" href="/products/<%= i %>"><%= i %></a>
                    </li>
                <% } %>
            </ul>
        </nav>
    </div>

    <div class="banner-container">
        <div class="banner-content">
            <h1>Plants The Perfect Choice!</h1>
            <p>Discount 20% Off for Lukas Members</p>
            <button onclick="window.location.href='/shop'">Discover Now</button>
        </div>
        <img src="/img/banner.png" alt="Banner">
    </div>
</body>
</html>
